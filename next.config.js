/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.momshome.in',
            'momshome.in',
            'cdn.shopify.com',
        ],
    },
};

module.exports = nextConfig;